This is a utility script for splitting an Inkscape SVG into multiple per-layer 
PNG images.

Installation:

    python setup.py install

Usage:

    python -m inkscape_split.py --dpi 180 my_image.svg
